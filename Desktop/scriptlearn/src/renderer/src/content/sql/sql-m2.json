{
  "id": "sql-m2",
  "title": "Filtrage et tri des données",
  "level": 8,
  "lang": "sql",
  "order": 2,
  "estimatedMinutes": 50,
  "course": [
    {
      "title": "Filtrer avec WHERE",
      "content": "Le clause `WHERE` permet de **filtrer les lignes** selon une condition :\n\n```sql\nSELECT * FROM employees\nWHERE department = 'IT';\n```\n\nOpérateurs de comparaison :\n- `=` égal, `!=` ou `<>` différent\n- `>` `<` `>=` `<=`\n- `IS NULL` / `IS NOT NULL`\n\nCombiner des conditions avec `AND` et `OR` :\n```sql\nWHERE salary > 50000 AND department = 'IT'\n```"
    },
    {
      "title": "Trier avec ORDER BY",
      "content": "Le clause `ORDER BY` trie les résultats :\n\n```sql\nSELECT name, salary FROM employees\nORDER BY salary DESC;\n```\n\n- `ASC` : ordre croissant (défaut)\n- `DESC` : ordre décroissant\n\nOn peut trier sur plusieurs colonnes :\n```sql\nORDER BY department ASC, salary DESC\n```"
    },
    {
      "title": "Filtres avancés : BETWEEN et LIKE",
      "content": "**BETWEEN** filtre une plage de valeurs :\n```sql\nWHERE salary BETWEEN 40000 AND 80000\n```\n\n**LIKE** effectue une recherche par motif :\n```sql\nWHERE name LIKE 'A%'   -- commence par A\nWHERE name LIKE '%son' -- finit par 'son'\nWHERE name LIKE '%ar%' -- contient 'ar'\n```\n\n**IN** filtre sur une liste de valeurs :\n```sql\nWHERE department IN ('IT', 'RH', 'Finance')\n```"
    }
  ],
  "exercises": [
    {
      "id": "ex-sql-m2-1",
      "order": 1,
      "title": "Filtrer par département",
      "instructions": "Récupérez tous les employés dont le `department` est `'IT'`.",
      "requiredKeywords": ["select", "from", "employees", "where", "department", "it"],
      "validationType": "keywords",
      "correction": "SELECT * FROM employees WHERE department = 'IT';",
      "explanation": "La clause `WHERE` s'évalue ligne par ligne. Seules les lignes satisfaisant la condition `department = 'IT'` sont retournées."
    },
    {
      "id": "ex-sql-m2-2",
      "order": 2,
      "title": "Condition avec AND",
      "instructions": "Trouvez les employés du département `'Finance'` dont le salaire (`salary`) est supérieur à `60000`.",
      "requiredKeywords": ["select", "from", "employees", "where", "finance", "and", "salary", "60000"],
      "validationType": "keywords",
      "correction": "SELECT * FROM employees WHERE department = 'Finance' AND salary > 60000;",
      "explanation": "`AND` exige que les **deux** conditions soient vraies. `OR` suffit qu'une seule le soit."
    },
    {
      "id": "ex-sql-m2-3",
      "order": 3,
      "title": "Trier par salaire décroissant",
      "instructions": "Récupérez `name` et `salary` de `employees`, triés par salaire du **plus élevé au plus bas**.",
      "requiredKeywords": ["select", "name", "salary", "from", "employees", "order by", "desc"],
      "validationType": "keywords",
      "correction": "SELECT name, salary FROM employees ORDER BY salary DESC;",
      "explanation": "`ORDER BY salary DESC` trie par ordre décroissant. Sans `DESC`, le tri serait croissant (`ASC` par défaut)."
    },
    {
      "id": "ex-sql-m2-4",
      "order": 4,
      "title": "Plage de valeurs avec BETWEEN",
      "instructions": "Trouvez les employés dont le salaire est **entre 40 000 et 70 000** (inclus).",
      "requiredKeywords": ["select", "from", "employees", "where", "salary", "between", "40000", "70000"],
      "validationType": "keywords",
      "correction": "SELECT * FROM employees WHERE salary BETWEEN 40000 AND 70000;",
      "explanation": "`BETWEEN a AND b` est équivalent à `>= a AND <= b`. Les bornes sont **incluses**."
    },
    {
      "id": "ex-sql-m2-5",
      "order": 5,
      "title": "Recherche avec LIKE",
      "instructions": "Trouvez tous les employés dont le nom (`name`) **commence par la lettre 'A'**.",
      "requiredKeywords": ["select", "from", "employees", "where", "name", "like", "a%"],
      "validationType": "keywords",
      "correction": "SELECT * FROM employees WHERE name LIKE 'A%';",
      "explanation": "Le `%` est un **joker** qui correspond à zéro ou plusieurs caractères. `'A%'` = commence par A. `'%A%'` = contient A."
    }
  ]
}
