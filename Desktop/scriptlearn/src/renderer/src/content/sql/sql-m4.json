{
  "id": "sql-m4",
  "title": "Sous-requêtes et vues",
  "level": 8,
  "lang": "sql",
  "order": 4,
  "estimatedMinutes": 55,
  "course": [
    {
      "title": "Sous-requêtes (subqueries)",
      "content": "Une **sous-requête** est une requête imbriquée dans une autre. Elle peut apparaître dans :\n\n- `WHERE` : filtrer selon un résultat calculé\n- `FROM` : utiliser un résultat comme table temporaire\n- `SELECT` : calculer une valeur par ligne\n\n```sql\n-- Employés gagnant plus que la moyenne\nSELECT name, salary\nFROM employees\nWHERE salary > (SELECT AVG(salary) FROM employees);\n```"
    },
    {
      "title": "IN, EXISTS et sous-requêtes corrélées",
      "content": "`IN` vérifie si une valeur est dans un ensemble :\n```sql\nSELECT name FROM employees\nWHERE department_id IN (SELECT id FROM departments WHERE budget > 100000);\n```\n\n`EXISTS` teste si une sous-requête retourne au moins une ligne :\n```sql\nSELECT name FROM employees e\nWHERE EXISTS (\n  SELECT 1 FROM projects p WHERE p.employee_id = e.id\n);\n```"
    },
    {
      "title": "Vues (CREATE VIEW)",
      "content": "Une **vue** est une requête sauvegardée comme table virtuelle. Elle simplifie les requêtes complexes et peut restreindre l'accès aux données.\n\n```sql\nCREATE VIEW high_earners AS\nSELECT name, salary, department\nFROM employees\nWHERE salary > 80000;\n\n-- Interroger la vue comme une table\nSELECT * FROM high_earners;\n```\n\nPour supprimer une vue : `DROP VIEW high_earners;`"
    }
  ],
  "exercises": [
    {
      "id": "ex-sql-m4-1",
      "order": 1,
      "title": "Employés au-dessus de la moyenne",
      "instructions": "Trouvez les employés dont le salaire est **supérieur au salaire moyen** de tous les employés.\n\nUtilisez une sous-requête avec `AVG(salary)`.",
      "requiredKeywords": ["select", "from", "employees", "where", "salary", "avg", "select"],
      "validationType": "keywords",
      "correction": "SELECT name, salary FROM employees WHERE salary > (SELECT AVG(salary) FROM employees);",
      "explanation": "La sous-requête `(SELECT AVG(salary) FROM employees)` est évaluée en premier, puis sa valeur est utilisée dans le `WHERE` de la requête principale."
    },
    {
      "id": "ex-sql-m4-2",
      "order": 2,
      "title": "Filtrer avec IN et sous-requête",
      "instructions": "Récupérez les employés dont le `department_id` est dans la liste des `id` de `departments` avec un `budget > 100000`.",
      "requiredKeywords": ["select", "from", "employees", "where", "department_id", "in", "select", "departments", "budget"],
      "validationType": "keywords",
      "correction": "SELECT * FROM employees WHERE department_id IN (SELECT id FROM departments WHERE budget > 100000);",
      "explanation": "`IN` avec une sous-requête est très lisible. Alternative : utiliser un `JOIN` avec un filtre `WHERE`."
    },
    {
      "id": "ex-sql-m4-3",
      "order": 3,
      "title": "Créer une vue",
      "instructions": "Créez une vue appelée `senior_employees` contenant les employés dont le salaire est supérieur à `80000`.\n\nIncluez `name`, `salary` et `department`.",
      "requiredKeywords": ["create view", "senior_employees", "as", "select", "from", "employees", "where", "salary", "80000"],
      "validationType": "keywords",
      "correction": "CREATE VIEW senior_employees AS SELECT name, salary, department FROM employees WHERE salary > 80000;",
      "explanation": "Une vue est stockée en base. Les utilisateurs peuvent interroger `senior_employees` sans connaître la requête sous-jacente, ce qui simplifie l'accès et améliore la sécurité."
    },
    {
      "id": "ex-sql-m4-4",
      "order": 4,
      "title": "Sous-requête dans le FROM",
      "instructions": "Calculez la **moyenne des salaires moyens** par département.\n\nIndice : d'abord calculer `AVG(salary)` par département dans une sous-requête, puis faire `AVG` dessus.",
      "requiredKeywords": ["select", "avg", "from", "employees", "group by", "department"],
      "validationType": "keywords",
      "correction": "SELECT AVG(avg_sal) FROM (SELECT AVG(salary) AS avg_sal FROM employees GROUP BY department) AS dept_avg;",
      "explanation": "La sous-requête dans `FROM` crée une table temporaire appelée `dept_avg`. La requête externe calcule la moyenne de ces moyennes."
    },
    {
      "id": "ex-sql-m4-5",
      "order": 5,
      "title": "EXISTS — employés avec projets",
      "instructions": "Trouvez les employés qui ont au moins un projet associé dans la table `projects`.\n\nLa table `projects` a une colonne `employee_id`. Utilisez `EXISTS`.",
      "requiredKeywords": ["select", "from", "employees", "where", "exists", "select", "from", "projects", "employee_id"],
      "validationType": "keywords",
      "correction": "SELECT name FROM employees e WHERE EXISTS (SELECT 1 FROM projects p WHERE p.employee_id = e.id);",
      "explanation": "`EXISTS` retourne `TRUE` dès qu'une ligne correspond dans la sous-requête. C'est souvent plus performant que `IN` sur de grandes tables car l'évaluation s'arrête dès le premier résultat trouvé."
    }
  ]
}
